#include <stdio.h>

int main(void)
{
    int value = getchar();
    int value1 = getchar();
    int value2 = getchar();
    putchar(value2);
    putchar(value1);
    putchar(value);// здесь продолжайте программу

    return 0;
}